# copy this file as secrets.env
# uncomment these following lines add create secrets. Be careful API_SECRET should be the same than MEDIASOUP_SECRET
#MEDIASOUP_SECRET=<use a random one>
#API_SECRET=MUST BE THE SAME THAN MEDIASOUP_SECRET
#APP_SECRET=<use a random one>
#JWT_SECRET=<use a random one>